apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: security.sec-policy1
  namespace: calico-system
spec:
  egress:
    - action: Allow
      destination: {}
      source: {}
  ingress:
    - action: Allow
      destination: {}
      source: {}
  tier: security
  types:
    - Ingress
    - Egress
