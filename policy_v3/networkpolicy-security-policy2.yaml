apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: sec-policy2
  namespace: calico-system
spec:
  egress:
    - action: Allow
      destination: {}
      source: {}
  ingress:
    - action: Allow
      destination: {}
      source: {}
  tier: security
  types:
    - Ingress
    - Egress
