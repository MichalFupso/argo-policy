apiVersion: projectcalico.org/v3
kind: StagedNetworkPolicy
metadata:
  name: staged-policy4
  namespace: calico-system
spec:
  types:
    - Ingress
    - Egress
  ingress:
    - action: Allow
  egress:
    - action: Allow
