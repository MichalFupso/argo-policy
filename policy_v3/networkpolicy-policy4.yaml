apiVersion: projectcalico.org/v3
kind: NetworkPolicy
metadata:
  name: policy4
  namespace: calico-system
spec:
  selector: all()
  egress:
    - action: Allow
      destination: {}
      source: {}
  ingress:
    - action: Allow
      destination: {}
      source: {}
  tier: default
  types:
    - Ingress
    - Egress
