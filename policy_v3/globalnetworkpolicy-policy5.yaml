apiVersion: projectcalico.org/v3
kind: GlobalNetworkPolicy
metadata:
  name: default.policy5
spec:
  egress:
    - action: Allow
      destination: {}
      source: {}
  ingress:
    - action: Allow
      destination: {}
      source: {}
  selector: all()
  tier: default
  types:
    - Ingress
    - Egress
