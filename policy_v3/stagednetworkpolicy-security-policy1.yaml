apiVersion: projectcalico.org/v3
kind: StagedNetworkPolicy
metadata:
  name: security.staged-sec-policy1
  namespace: calico-system
spec:
  types:
    - Ingress
    - Egress
  ingress:
    - action: Allow
      source: {}
      destination: {}
  egress:
    - action: Allow
      source: {}
      destination: {}
