apiVersion: projectcalico.org/v3
kind: GlobalNetworkPolicy
metadata:
  name: security.sec-policy1
spec:
  egress:
    - action: Allow
      destination: {}
      source: {}
  ingress:
    - action: Allow
      destination: {}
      source: {}
  tier: security
  types:
    - Ingress
    - Egress
